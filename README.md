# Magister

Unofficial clients for [Magister](https://www.magister.nl). This project centers around a shared Magister package that handles all core Magister functionality, with implementations for a CLI, TUI, and MCP server. All components are in an early stage and are largely generated by AI agents.

## Layout

```
apps/mcli          JSON CLI (mcli)
apps/mtui          Interactive TUI (mtui)
apps/mcp           Next.js dashboard + remote MCP server
packages/shared    Magister client, auth, and presenters
```

`@magister/shared` talks to Magister's APIs and owns token storage. The apps are thin wrappers around it.

## Requirements

- [Bun](https://bun.sh) 1.3.10 or later
- A Magister school account

From the repo root:

```bash
bun install
bun run setup
```

`setup` walks through school URL, username, and password, then writes tokens to `~/.config/magister/tokens.json`. That file is shared by `mcli` and `mtui`. Override the path with `MAGISTER_TOKENS_FILE` if you need to.

A leftover `tokens.json` in the working directory is still picked up as a fallback. Prefer the global file.

## CLI

`mcli` prints JSON to stdout. Useful for scripts and agents.

```bash
bun run mcli capabilities
bun run mcli account
bun run mcli schedule --from today --to today
bun run mcli grades
bun run mcli messages --limit 20
bun run mcli message 12345 --attachments
bun run mcli assignments
bun run mcli assignment 67890
bun run mcli study-guides
bun run mcli study-guide 111
```

Dates accept `today`, `tomorrow`, or `YYYY-MM-DD`. Errors also come back as JSON with a non-zero exit code.

Compile a standalone binary with `bun run build:mcli` (writes `dist/mcli`).

## Terminal UI

```bash
bun run tui
```

Interactive views for schedule, grades, messages, and assignments. Login lives in the TUI if tokens are missing. `q` or Ctrl+C exits.

## MCP server

The `apps/mcp` app is a Next.js dashboard (Google sign-in, then a Magister OAuth connect) plus an authenticated remote MCP endpoint at `/api/mcp`. Clients send `Authorization: Bearer mag_mcp_...`. OAuth-only MCP clients are not supported.

See [apps/mcp/README.md](apps/mcp/README.md) for env vars, local setup, and Vercel deploy.
Vercel is the recommended deployment target. The MCP server is self-hosted: operators are
responsible for their deployment, stored data, retention policy, and access controls.

```bash
bun run dev:mcp
```

Tools: `get_account`, `get_schedule`, `get_grades`, `list_messages`, `get_message`, `list_assignments`, `get_assignment`, `list_study_guides`, `get_study_guide`.

## Shared package

`@magister/shared` is the only place that talks to Magister.

| Export | What it is |
| --- | --- |
| `@magister/shared` | Client, auth helpers, presenters, setup |
| `@magister/shared/magister` | `MagisterClient` and token helpers |
| `@magister/shared/presenters` | Normalized API shapes |
| `@magister/shared/setup` | Interactive `runSetup()` |

## Scripts

| Command | What it does |
| --- | --- |
| `bun run setup` | Interactive Magister login |
| `bun run mcli …` | Run a CLI command |
| `bun run tui` | Open the TUI |
| `bun run dev:mcp` | Next.js MCP app on localhost:3000 |
| `bun run build:release` | Compile the CLI and TUI release binaries |
| `bun run build` | Compile the CLI, TUI, and configured MCP app |
| `bun run test` | Run the shared and MCP unit tests |
| `bun run typecheck` | Type-check the monorepo |

Tagged releases are built for Linux, macOS, and Windows by GitHub Actions. They contain only
the `mcli` and `mtui` binaries; deploy the MCP app through Vercel instead.

## Tokens

Treat `tokens.json` like a password. Rotating Magister credentials or wiping `~/.config/magister/tokens.json` means running `bun run setup` again.

## Disclaimer

This project is not affiliated with, endorsed by, or connected to Magister or Schoologica B.V. in any way.

Use this app at your own risk. The author is not responsible for any consequences, damages, data loss, account issues, or other outcomes caused by using this app.

## License

Licensed under the [MIT License](LICENSE).
